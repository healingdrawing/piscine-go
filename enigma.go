package piscine

func Enigma(a ***int, b *int, c *******int, d ****int) {
	// a into c. "c" will have "a" value
	// c into d.
	// d into b.
	// b into a.

	aa, cc, dd, bb := ***a+0, *******c+0, ****d+0, *b+0
	*******c, ****d, *b, ***a = aa, cc, dd, bb
}
